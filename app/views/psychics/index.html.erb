<div id="half-fold-purple" class="d-flex flex-column justify-content-center align-items-center">
  <div><h1> Here are all our great psychics !</h1></div>
  <div><h2>Have a look to find the one who will bring you some magic...</h2></div>
  <div class="d-flex" id="search-form">
  <%= form_tag psychics_path, method: :get do %>
    <%= submit_tag "Search", class: "btn btn-primary", id:"search-submit" %>
    <%= text_field_tag :query, params[:query], class: "form-control", id: "search-input", placeholder: "Find a psychic" %>
  </div>
<% end %>
</div>

<div class="container">

    <div style="margin-top: -70px">
      <div class="cards">
        <% @psychics.each do |psychic| %>
            <div class="card-index">
              <%= link_to psychic_path(psychic) do %>
                <% if psychic.photo.attached? %>
                  <%= cl_image_tag psychic.photo.key, crop: :pad %>
                <% else %>
                  <img src='https://source.unsplash.com/1600x900/?photos,portrait'/>
                <% end %>
                <div class="card-int">
                  <h3><%= psychic.user.name%> <%= psychic.user.last_name%></h3>
                  <p><i class="fas fa-map-marker-alt"></i> <%= psychic.localisation %></p>
                  <div class='d-flex justify-content-end'>
                    <div>
                      <p><strong>€<%= psychic.hour_rate %></strong>/hour</p>
                    </div>
                  </div>
                </div>
              <% end %>
            </div>
        <% end %>
      </div>
    </div>
</div>
